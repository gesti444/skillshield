# Contributing

Keep rules focused, explain the threat model, and include safe positive and negative test cases. Never commit live credentials, malicious payloads, or private client data. Run `python3 -m unittest discover -s tests -v` before opening a pull request.
